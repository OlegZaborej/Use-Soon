{
  "name": "smart-pantry-monorepo",
  "private": true,
  "scripts": {
    "install": "npm --prefix backend install && npm --prefix frontend install",
    "dev": "echo 'Run backend and frontend in separate terminals: npm --prefix backend run dev / npm --prefix frontend run dev'",
    "build": "npm --prefix backend run build && npm --prefix frontend run build",
    "prisma:migrate": "npm --prefix backend run prisma:migrate",
    "prisma:seed": "npm --prefix backend run prisma:seed"
  }
}
